export default class Mode {
  constructor({icon, iconText, fontSize, lineHeight, color, background}) {
    this.icon = icon
    this.iconText = iconText
    this.fontSize = fontSize
    this.lineHeight = lineHeight
    this.color = color
    this.background = background
  }
}